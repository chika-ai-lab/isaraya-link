[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[secrets_scanning]
  omit_keys = ["NEXT_SUPABASE_URL", "NEXT_SUPABASE_PROJECT_ID", "NEXT_SUPABASE_PUBLISHABLE_KEY"]
